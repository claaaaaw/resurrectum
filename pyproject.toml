[project]
name = "resurrectum"
version = "0.1.0"
description = "Resurrectum v1 (OpenClaw-only) tooling"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "argon2-cffi>=23.1.0",
  "boto3>=1.34.0",
  "cryptography>=42.0.0",
  "jsonschema>=4.21.0",
  "rfc8785>=0.1.4",
]

[project.optional-dependencies]
test = [
  "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[project.scripts]
summon-validate-examples = "resurrectum.summon.scripts.validate_examples:main"

[build-system]
requires = ["hatchling>=1.24.2"]
build-backend = "hatchling.build"
